# Deliberately does NOT pull in sentence-transformers/torch from
# requirements.txt -- the embedder falls back to a local hashing embedding
# when that package isn't installed (see app/embeddings.py), which keeps
# CI/local test runs fast. The full requirements.txt (used by the Docker
# image) installs the real embedding model.
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.5.2
redis==5.0.8
httpx==0.27.2
numpy==1.26.4
python-multipart==0.0.9

pytest==8.3.3
pytest-asyncio==0.24.0
fakeredis==2.24.1
ruff==0.6.9
